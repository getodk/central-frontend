import type { XPathDOMAdapter } from '@getodk/xpath';

/**
 * @todo This will be implemented shortly! It's being added now so it can be
 * referenced by JSDoc in modules added in a prerequisite commit.
 */
// @ts-expect-error - unknown is not assignable to XPathNode, to be updated shortly!
export interface EngineDOMAdapter extends XPathDOMAdapter<unknown> {}
